#ifndef EXTRA_HPP
#define EXTRA_HPP

#include "Server.hpp"
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
class User;


void ErrorMsg(int fd, const std::string &message, const std::string &key);

#endif
